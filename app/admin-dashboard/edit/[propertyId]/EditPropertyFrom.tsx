"use client";

import type { Property } from "@/types/property";

export default function EditPropertyFrom({
  id,
  address1,
  address2,
  bedrooms,
  bathrooms,
  price,
  status,
}: Property) {
  return <div>EditPropertyFrom</div>;
}
